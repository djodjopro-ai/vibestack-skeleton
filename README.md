# vibestack-skeleton

Skeleton template for Vibestack-generated AI-assistant apps. Generated apps clone from this at a pinned semver tag.

Ships with: auth, subscription stubs, chat+agent loop, core fs tools, Telegram bridge scaffolding, UI kit, theme, WebSocket realtime.

Domain layer (sections, tools, schema) is pluggable — see `docs/seams.md`.
